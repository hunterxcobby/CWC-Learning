#include <stdbool.h>
#include <stdio.h>

int main (void)
{
    bool isDianaPresent = true;
    bool isAkwesiPresent = false;

    if (!isDianaPresent)
    {
        printf("Tell her to mark the books\n");
    }
    else
    {
        printf("Tell me Diana is not present.\n");
    }

    

    return(0);
}